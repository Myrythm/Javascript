const arr1 = ['udin', 'arip', 'ucup', 'aji'];
const arr2 = ['saep', 'rum', 'adi', 'asep'];
const arr3 = ['sopyan', 'ari'];

const gabung = arr1.concat(arr2, arr3);

for (let i = 0; i < gabung.length; i++) {
    console.log(gabung[i],);
    
}